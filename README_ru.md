# Приложение для Доставки

<h2>🌐</h2>
<ul>
  <li><a href="https://github.com/SamuelRocha91/delivery_back" target="_blank">Português</a></li>
  <li><a href="https://github.com/SamuelRocha91/delivery_back/blob/main/README_es.md" target="_blank">Español</a></li>
  <li><a href="https://github.com/SamuelRocha91/delivery_back/blob/main/README_en.md" target="_blank">English</a></li>
  <li><a href="https://github.com/SamuelRocha91/delivery_back/blob/main/README_ru.md" target="_blank">Русский</a></li>
  <li><a href="https://github.com/SamuelRocha91/delivery_back/blob/main/README_ch.md" target="_blank">中文</a></li>
  <li><a href="https://github.com/SamuelRocha91/delivery_back/blob/main/README_ar.md" target="_blank">العربية</a></li>
</ul>

Этот репозиторий представляет собой Rails-бэкенд для приложения по доставке, включая интерфейс управления данными для пользователей с ролями администратора и разработчика.

![Иллюстрация использования интерфейса администратора](./assets/admin.gif)

## Связанные Репозитории

- [Consumy Application](https://github.com/SamuelRocha91/consumy) - Приложение для потребителей
- [Seller Application](https://github.com/SamuelRocha91/seller_application) - Приложение для продавцов
- [Paymenty API](https://github.com/SamuelRocha91/paymenty) - API для платежей

### Понятия и используемые библиотеки (gems)

Этот проект охватывает и использует следующие понятия и библиотеки (gems):

- **Фоновые процессы**: Использование **Sidekiq** для асинхронной обработки задач. **Redis** используется в качестве очереди для этих задач, что позволяет выполнять длительные операции, такие как отправка электронной почты или обработка данных, вне основного цикла приложения, улучшая производительность и пользовательский опыт.

- **Пагинация**: Реализация пагинации для списка ресурсов с использованием библиотеки **Kaminari**, что упрощает работу с большими объемами данных и позволяет делить списки на страницы для улучшения навигации.

- **Тестирование с помощью RSpec и Shoulda Matchers**: Использование **RSpec** для написания модульных, интеграционных и функциональных тестов. Библиотека **Shoulda Matchers** упрощает утверждения, особенно при тестировании валидации и ассоциаций ActiveRecord, делая тесты более читаемыми и эффективными.

- **Покрытие тестов с помощью SimpleCov**: Интеграция с библиотекой **SimpleCov** для автоматизированного анализа покрытия кода тестами. Это гарантирует, что критические области приложения протестированы, предоставляя отчеты о том, какие части кода еще нужно покрыть.

- **Мягкое удаление с Discard**: Реализация мягкого удаления с помощью библиотеки **Discard**, что позволяет "удалять" записи, не удаляя их физически из базы данных. Это дает возможность восстанавливать данные в будущем и сохраняет их историческую целостность.

- **Bullet для обнаружения N+1 запросов**: Библиотека **Bullet** используется для обнаружения и предотвращения проблем с производительностью, таких как **N+1 запросы**. Bullet уведомляет разработчика, когда запрос может быть оптимизирован, предлагая использовать **жадную загрузку (eager loading)** для предотвращения множества ненужных запросов к базе данных, что улучшает производительность приложения.

- **Анонимизация данных**: Применение техник анонимизации конфиденциальных данных для соблюдения требований конфиденциальности и безопасности, таких как **LGPD** (Закон о защите персональных данных). Используются такие техники, как маскирование и хеширование данных.

- **RESTful API**: Разработка **RESTful API**, следуя принципам архитектуры программного обеспечения, что обеспечивает эффективную связь между серверной и клиентской частями. API разработан для того, чтобы быть масштабируемым, модульным и легко поддерживаемым.

- **Swagger**: Автоматическая документация API с использованием библиотеки **Rswag** (основанной на Swagger), что упрощает понимание и использование API, предоставляя интерактивный интерфейс для тестирования маршрутов, параметров и ответов прямо в браузере.

- **Docker**: Возможность контейнеризации с помощью **Docker**, что упрощает разработку, тестирование и развертывание приложения в контролируемых средах. Это гарантирует, что зависимости и инфраструктура изолированы и могут быть легко воспроизведены на разных машинах.

- **Обработка изображений**: Использование библиотеки **ImageProcessing** для обработки изображений, таких как изменение размера и сжатие. Инструменты, такие как **ruby-vips**, используются для оптимизации обработки больших объемов изображений, обеспечивая высокую производительность.

- **WebSockets и SSE**: Реализация связи в реальном времени с использованием **WebSockets** для функций, требующих немедленного взаимодействия, таких как чаты, уведомления или обновления в реальном времени. **Server-Sent Events (SSE)** также используются для односторонней передачи обновлений от сервера к клиенту.

- **Расчет расстояний**: Использование библиотеки **Geocoder** для расчета расстояний между клиентом и коммерческим заведением, что упрощает предоставление услуг на основе геолокации. Расчеты расстояний выполняются в километрах, что обеспечивает точность и эффективность.

- **Потоки**: Использование **потоков (threads)** для улучшения конкуренции и производительности в параллельных операциях, таких как обработка больших объемов данных или интеграция с внешними сервисами. Потоки позволяют выполнять несколько операций одновременно, оптимизируя время отклика приложения.

- **Управление сессиями и аутентификация**: Аутентификация пользователей осуществляется с помощью **Devise**, популярной библиотеки в экосистеме Rails. Также поддерживается аутентификация с использованием **JWT** (JSON Web Token), особенно полезная для токенов в API.

- **Управление CORS**: Использование библиотеки **rack-cors** для управления разрешениями **Cross-Origin Resource Sharing (CORS)**, что обеспечивает безопасный доступ к API с различных доменов.

- **Машины состояний с State Machines-Activerecord**: Использование библиотеки **state_machines-activerecord** для реализации **машин состояний** в моделях ActiveRecord. Это позволяет контролировать переходы между состояниями, обеспечивая выполнение определенных процессов и потоков, таких как изменения в статусах заказов, платежей или автоматизированных процессов.

- **Faraday для внешних API**: Интеграция с внешними сервисами с помощью библиотеки **Faraday**, которая представляет собой HTTP клиент для выполнения REST-запросов гибким и эффективным способом. Это упрощает взаимодействие с API третьих сторон, предлагая поддержку промежуточного ПО, повторных попыток и обработки ошибок.

- **Автоматизация задач и DevOps**: Структура **DevOps** поддерживается такими инструментами, как **Sidekiq** и **Docker**, для автоматизации процессов непрерывной интеграции, развертывания и мониторинга фоновых задач.

## Настройка Проекта

### С Docker

Чтобы запустить этот проект с помощью Docker, выполните следующие шаги:

#### Предварительные Требования

Убедитесь, что у вас установлены Docker и Docker Compose.

- [Docker](https://docs.docker.com/get-docker/)
- [Docker Compose](https://docs.docker.com/compose/install/)

#### Конфигурация

1. Клонируйте связанные репозитории:

   ```sh
   git clone https://github.com/SamuelRocha91/consumy.git
   git clone https://github.com/SamuelRocha91/seller_application.git
   git clone https://github.com/SamuelRocha91/paymenty.git
   git clone https://github.com/SamuelRocha91/delivery_back.git
   ```

2. Скачайте файл `docker-compose.yml` для настройки необходимых сервисов Docker и поместите его в корневую папку, где были клонированы репозитории:

   - [Скачать docker-compose.yml](https://drive.google.com/file/d/1kzs-DJGCvYImBQAqr1GI-zwoNha_b8tA/view?usp=drive_link)

3. В корневой папке проекта, где находится файл `docker-compose.yml`, выполните следующую команду для сборки и запуска сервисов:

   ```sh
   docker-compose up --build
   ```

4. При доступе к бэкенд-приложению в браузере нажмите на "RUN PENDING MIGRATES", чтобы создать миграции.

5. Рекомендуется войти в контейнер бэкенд-приложения и запустить сиды:

   ```sh
   docker exec -it backendContainerName /bin/sh 
   rails db:seed
   ```

6. Необходимо настроить переменные окружения в файле .env внутри приложения delivery_back. Пример:

   ```sh
   JWT_SECRET_KEY=xxxxxxxxxxxxxxxxxxxxx
   ```

7. Необходимо настроить переменные окружения в файле .env.development внутри приложений consumy и seller. Пример:

   ```sh
   VITE_BASE_URL=http://localhost:3000
   VITE_X_API_KEY=Z34dqYJ8qBelcmcBfWNuHc5JB/w=
   ```

**ПРИМЕЧАНИЕ** API-ключи могут быть сгенерированы внутри контейнера бэкенда через консоль, а затем вставлены в файл .env.

**ПРИМЕЧАНИЕ** Другой способ - сгенерировать их через пользователя с ролью разработчика в интерфейсе бэкенда и скопировать и вставить в соответствующее приложение.

**ПРИМЕЧАНИЕ** Не забудьте перезапустить контейнеры, чтобы переменные были загружены.

![Пример конфигурации](./assets/apikey.gif)

### Без Docker

Чтобы настроить и запустить проект локально без Docker, выполните следующие шаги:

#### Предварительные Требования

Убедитесь, что Redis и Sidekiq настроены и работают локально для правильного функционирования приложения. Также установите необходимые зависимости R:

```sh
install.packages("FactoMineR")
install.packages("ggplot2")
install.packages("reshape2")
```

#### Установка Зависимостей

```sh
bundle install
```

#### Настройка Базы Данных

Создайте базу данных и выполните миграции:

```sh
rails db:create
rails db:migrate
```

#### Запуск Локального Сервера

Чтобы запустить сервер локально:

```sh
rails server
```

#### Запуск Тестов

Чтобы выполнить автоматизированные тесты:

```sh
bundle exec rspec
```

#### Запуск Redis и Sidekiq

Убедитесь, что Redis установлен и работает. Запустите Redis с помощью следующей команды:

```sh
redis-server
```

Затем запустите Sidekiq:

```sh
bundle exec sidekiq
```

---

Этот проект был создан как часть оценочного требования для программы Quero Ser Dev 9-го выпуска от LWSA.

---
