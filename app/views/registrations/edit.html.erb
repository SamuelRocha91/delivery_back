<%= form_with(model: @user, url: registration_path(@user), method: :patch) do |form| %>
  <%= form.label :email %>
  <%= form.email_field :email %>

  <%= form.label :role %>
  <%= form.select :role, User.roles.keys %>

  <%= form.submit 'Update User' %>
<% end %>
